# Role
You are a Senior Front-End Engineer specializing in accessibility, performance, and minimalist design. You prefer raw HTML/CSS over heavy frameworks.

# Task
Generate the complete `index.html` file for the "Zero Delta LLC" landing page based on the specifications below.

# Context
This site is for an IT/Cloud Architect. It needs to look like a clean, modern terminal interface. It must load instantly and be fully responsive on mobile devices.

# Technical Constraints
1.  **Single File:** All CSS must be embedded in the `<style>` tag within the `<head>`.
2.  **No External Dependencies:** No Google Fonts, no FontAwesome, no CDNs.
3.  **Clean Code:** Use semantic HTML5 tags (`<main>`, `<footer>`, `<header>`).

# Visual Design Specs
* **Background:** `#0d1117`
* **Text Color:** `#c9d1d9`
* **Font:** System Monospace (`'Courier New', Courier, monospace`).
* **Container:** Max-width 600px, vertically and horizontally centered on desktop, 20px padding on mobile.
* **Borders:** Subtle borders (`1px solid #30363d`) around the main container or code blocks.

# Required Content Elements
1.  **Header:** * H1 tag: `> ZERO DELTA LLC`
    * Add a pure CSS blinking animation to a cursor `_` at the end of the H1.
2.  **Sub-Header:** "Cloud Architecture & Infrastructure Automation"
3.  **Status Line:** A small green circle (CSS only) next to the text "Status: OPERATIONAL".
4.  **Links Section:** * Email: `dave@zdelta.dev`
    * GitHub: `github.com/dave-schmidt-dev`
    * LinkedIn: `linkedin.com/in/dave-schmidt-dev`
5. Footer:
   - "Registered: Commonwealth of Virginia"
   - "C2C Ready | W-9 Available on Request"

# Output Requirement
Provide **only** the raw code block for the `index.html` file. Do not provide explanation text unless asking a clarifying question.